{{#each this}}
<h1>{{name}}</h1>
<p>Capital: {{capital}}</p>
<p>Population: {{population}}</p>
<ul>Languages:
    {{#each languages}}
    <li>{{name}}</li>
    {{/each}}
</ul>
<img src="{{flag}}" alt="flag">
{{/each}}